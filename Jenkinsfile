pipeline {
    agent any

    stages {
        stage('Calculate 2 + 2') {
            steps {
                script {
                    int result = 2 + 2
                    println("The result of 2 + 2 is: ${result}")
                }
            }
        }
    }
}
